<%= form_with(model: amiga, local: true) do |form| %>
  <% if amiga.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(amiga.errors.count, "error") %> prohibited this amiga from being saved:</h2>

      <ul>
      <% amiga.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :bday %>
    <%= form.date_select :bday %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
